# **Chỉ mục (Index)**

## Mục lục

- [**Chương 17. Chỉ mục (Index)**](#chỉ-mục-index)

**Ký hiệu (Symbols)**

! (dấu chấm than) vô hiệu hóa kiểm tra khởi tạo, Các thuộc tính được gán chắc chắn (Definitely assigned properties)

khẳng định kiểu không-null (non-null type assertions), Khẳng định không null (Non-Null Assertions)

# (dấu thăng), các thành viên lớp private, Phạm vi hiển thị của thành viên (Member Visibility)

& (dấu và), kiểu giao (intersection types), Kiểu giao (Intersection Types)

() (dấu ngoặc đơn) mảng và, Kiểu mảng và hàm (Array and Function Types)

trong kiểu hàm, Dấu ngoặc đơn của kiểu hàm (Function Type Parentheses)

... (dấu ba chấm), toán tử spread, Tham số rest cho mảng, Spreads

tuples làm tham số rest, Tuples làm tham số rest (Tuples as rest parameters)

? (dấu chấm hỏi) tham số tùy chọn, Tham số tùy chọn (Optional Parameters)

thuộc tính tùy chọn, Thuộc tính tùy chọn (Optional Properties)

chú thích @ts-check, @ts-check

| (dấu gạch đứng) toán tử, Kiểu hợp (Union Types)

**A**

lớp trừu tượng (abstract classes) mô tả, Lớp trừu tượng (Abstract Classes)

tìm kiếm triển khai của, Tìm kiếm triển khai (Finding Implementations)

các bổ từ kiểm soát truy cập, thay đổi, Thay đổi các bổ từ (Changing Modifiers)

tùy chọn trình biên dịch allowJs, allowJs

tùy chọn trình biên dịch allowSyntheticDefaultImports, allowSyntheticDefaultImports

ambient contexts, Các tệp khai báo (Declaration Files), Khai báo các giá trị thời gian chạy (Declaring Runtime Values)

dấu và (&), kiểu giao (intersection types), Kiểu giao (Intersection Types)

kiểu any

ambient contexts, Khai báo các giá trị thời gian chạy (Declaring Runtime Values)

mô tả, any, một lần nữa (any, Again)

evolving any, Chú thích kiểu (Type Annotations), Mảng evolving any (Evolving Any Arrays)

tùy chọn trình biên dịch noImplicitAny, noImplicitAny

tùy chọn trình biên dịch useUnknownInCatchVariables, useUnknownInCatchVariables

mảng (arrays) mô tả, Mảng (Arrays)

kết hợp với toán tử spread, Spreads

các thành viên, truy xuất, Thành viên mảng (Array Members) - Lưu ý: Các thành viên không an toàn

tham số rest dưới dạng mảng, Tham số rest (Rest Parameters)

tuples

khả năng gán, Khả năng gán của Tuple (Tuple Assignability)

khẳng định const, Khẳng định const cho Tuples (Const asserted tuples)

mô tả, Tuples

kiểu tường minh, Kiểu tuple tường minh (Explicit tuple types)

suy luận, Suy luận Tuple (Tuple Inferences)

làm tham số rest, Tuples làm tham số rest (Tuples as rest parameters)

dưới dạng kiểu dữ liệu, Mảng evolving any (Evolving Any Arrays)

kiểu hàm và mảng, Kiểu mảng và hàm (Array and Function Types)

mảng đa chiều, Mảng đa chiều (Multidimensional Arrays)

chú thích kiểu cho mảng, Kiểu mảng (Array Types)

kiểu hợp và mảng, Mảng kiểu hợp (Union-Type Arrays)

toán tử as const, Khẳng định const cho Tuples (Const asserted tuples)

từ khóa as, Khẳng định kiểu (Type Assertions)

khẳng định (assertions) (xem const assertions; type assertions)

khả năng gán (assignability)

mô tả, Khả năng gán (Assignability)

lỗi khả năng gán, Hiểu các lỗi khả năng gán (Understanding Assignability Errors)

cho kiểu hàm, Kiểu hàm (Function Types)

cho kiểu giao, Lỗi khả năng gán dài (Long assignability errors)

của literals, Khả năng gán Literal (Literal Assignability)

của các lớp con, Khả năng gán của lớp kế thừa (Extension Assignability)

của tuples, Khả năng gán của Tuple (Tuple Assignability)

của khẳng định kiểu, Khả năng gán của khẳng định (Assertion assignability)

thu hẹp phép gán (assignment narrowing), Thu hẹp phép gán (Assignment Narrowing)

hàm async (async functions), Promises và, Hàm Async (Async Functions)

tự động hoàn thành khi viết mã (autocompletion), Công cụ phát triển mạnh mẽ hơn (Stronger Developer Tooling), Tự động hoàn thành tên (Completing Names)

giá trị số tự động với enums, Giá trị số tự động (Automatic Numeric Values)

**B**

Babel, Cú pháp biên dịch, Xuất mã (Emit), Chỉ Import và Export Kiểu (Type-Only Imports and Exports)

kiểu nguyên thủy bigint, Có gì trong một Kiểu? (What's in a Type?)

sai lầm tỷ đô (billion-dollar mistake), Kiểm tra null nghiêm ngặt (Strict Null Checking) - Sai lầm tỷ đô (The Billion-Dollar Mistake)

tham số hàm hai chiều (bivariant function parameters), strictFunctionTypes

kiểu boolean, Có gì trong một Kiểu? (What's in a Type?)

kiểu đáy (bottom types) mô tả, Kiểu đối tượng (Object Types), Kiểu đáy never (The never Bottom Type), Kiểu đỉnh (Top Types)

never, never

chế độ build (build mode) cho tham chiếu dự án, Chế độ Build (Build Mode)

các khai báo tích hợp sẵn (built-in declarations)

khai báo DOM, Khai báo DOM (DOM Declarations)

khai báo thư viện, Khai báo thư viện (Library Declarations)

mục tiêu thư viện, Mục tiêu thư viện (Library targets)

**C**

chữ ký cuộc gọi trong interfaces (call signatures in interfaces), Chữ ký cuộc gọi (Call Signatures)

tùy chọn trình biên dịch checkJs, checkJs, @ts-check

generics của lớp cho mảng, Kiểu mảng (Array Types)

lớp (classes)

trừu tượng (abstract), Lớp trừu tượng (Abstract Classes)

tìm kiếm triển khai của, Tìm kiếm triển khai (Finding Implementations)

như kiểu dữ liệu, Lớp với vai trò là Kiểu dữ liệu (Classes as Types)

mở rộng (extending), Mở rộng một Lớp (Extending a Class)

khả năng gán của kế thừa, Khả năng gán của lớp kế thừa (Extension Assignability)

hàm khởi tạo bị ghi đè, Ghi đè hàm khởi tạo (Overridden Constructors)

phương thức bị ghi đè, Ghi đè phương thức (Overridden Methods)

thuộc tính bị ghi đè, Ghi đè thuộc tính (Overridden Properties)

generics, Lớp generic (Generic Classes)

kiểu lớp generic tường minh, Các kiểu lớp generic tường minh (Explicit Generic Class Types)

mở rộng lớp generic, Kế thừa các lớp generic (Extending Generic Classes)

triển khai giao diện generic, Triển khai các giao diện generic (Implementing Generic Interfaces)

generics của phương thức, Phương thức generic (Method Generics)

generics của lớp tĩnh, Generics trên thành viên tĩnh (Static Class Generics)

interfaces và, Lớp và Giao diện (Classes and Interfaces)

triển khai nhiều interfaces, Triển khai nhiều giao diện (Implementing Multiple Interfaces)

phạm vi hiển thị của thành viên, Phạm vi hiển thị của thành viên (Member Visibility)

các bổ từ trường tĩnh, Các bổ từ trường tĩnh (Static Field Modifiers)

phương thức, Các phương thức của lớp (Class Methods)

thuộc tính tham số, Thuộc tính tham số của lớp (Class Parameter Properties)

thuộc tính, Các thuộc tính của lớp (Class Properties)

thuộc tính hàm, Thuộc tính hàm (Function Properties)

kiểm tra khởi tạo, Kiểm tra khởi tạo (Initialization Checking)

thuộc tính tùy chọn, Thuộc tính tùy chọn (Optional Properties)

thuộc tính chỉ đọc, Thuộc tính chỉ đọc (Read-Only Properties)

tùy chọn dòng lệnh (CLI options)

tùy chọn chế độ build, Các tùy chọn ở chế độ Build (Build-mode options)

so với tệp cấu hình, CLI so với tệp cấu hình (CLI Versus Configuration)

tùy chọn tsc, Tùy chọn tsc (tsc Options)

chế độ pretty, Chế độ Pretty (Pretty Mode)

chế độ watch, Chế độ Watch (Watch Mode)

hành động mã nguồn (code actions)

mô tả, Hành động mã nguồn (Code Actions)

sửa lỗi nhanh khác, Các sửa lỗi nhanh khác (Other quick fixes)

tái cấu trúc, Tái cấu trúc (Refactoring)

xóa mã không sử dụng, Xóa mã không sử dụng (Removing unused code)

đổi tên, Đổi tên (Renaming)

biên dịch mã (compiling code), Bắt đầu với TypeScript (Getting Started with TypeScript), Biên dịch cú pháp (Compiling Syntax), Tùy chọn tsc (tsc Options)

tùy chọn trình biên dịch composite, composite

kiểu điều kiện (conditional types)

mô tả, Kiểu điều kiện (Conditional Types)

tổng quát (generic), Kiểu điều kiện tổng quát (Generic Conditional Types)

kiểu suy luận, Các kiểu được suy luận (Inferred Types)

kiểu điều kiện ánh xạ (mapped conditional types), Mapped Conditional Types

never và, never và Kiểu điều kiện (never and Conditional Types)

tính phân phối kiểu, Tính phân phối của kiểu (Type Distributivity)

mở rộng cấu hình (configuration extensions)

cấu hình cơ sở, Cấu hình cơ sở (Configuration Bases)

tùy chọn extends, extends

tùy chọn cấu hình (configuration options)

các phần mở rộng thay thế, Các phần mở rộng thay thế (Alternative Extensions)

tùy chọn tsc, Tùy chọn tsc (tsc Options)

xuất mã (emit), Xuất mã (Emit)

phần mở rộng cấu hình, Mở rộng cấu hình (Configuration Extensions)

bao gồm tệp, Bao gồm tệp (File Inclusions)

JavaScript, JavaScript

các module, Các Module (Modules)

tham chiếu dự án, Tham chiếu dự án (Project References)

các tệp TSConfig, Các tệp TSConfig (TSConfig Files)

kiểm tra kiểu, Kiểm tra kiểu (Type Checking)

khẳng định const (const assertions)

mô tả, Khẳng định const (Const Assertions)

literals sang primitives, Từ Literals đến Primitives (Literals to Primitives)

đối tượng chỉ đọc, Các đối tượng chỉ đọc (Read-Only Objects)

tuples, Khẳng định const cho Tuples (Const asserted tuples)

const enums, Const Enums

hàm khởi tạo (constructors)

lớp, Các phương thức của lớp (Class Methods)

ghi đè, Ghi đè hàm khởi tạo (Overridden Constructors)

tham số kiểu có ràng buộc (constrained type parameters)

mô tả, Ràng buộc kiểu generic (Constrained Generic Types)

keyof và, keyof và các tham số kiểu có ràng buộc (keyof and Constrained Type Parameters)

**D**

tùy chọn trình biên dịch declaration, Tùy chọn declaration (declaration), Xuất các tệp khai báo (Emitting Declarations)

tệp khai báo (declaration files)

các khai báo tích hợp sẵn, Các khai báo tích hợp sẵn (Built-In Declarations)

khai báo DOM, Khai báo DOM (DOM Declarations)

khai báo thư viện, Khai báo thư viện (Library Declarations)

khai báo các giá trị thời gian chạy, Khai báo các giá trị thời gian chạy (Declaring Runtime Values)

bổ sung toàn cục, Bổ sung toàn cục (Global Augmentations)

hợp nhất interface toàn cục, Hợp nhất giao diện toàn cục (Global Interface Merging)

các giá trị toàn cục, Các giá trị toàn cục (Global Values)

DefinitelyTyped, DefinitelyTyped

mô tả, Các tệp khai báo (Declaration Files)

tùy chọn emitDeclarationOnly, emitDeclarationOnly

khai báo module, Khai báo Module (Module Declarations)

kiểu gói (package types), Kiểu của các gói (Package Types)

tùy chọn trình biên dịch declarationMap, declarationMap

từ khóa declare, Khai báo các giá trị thời gian chạy (Declaring Runtime Values), Khai báo Module (Module Declarations)

decorators, Decorators thử nghiệm (Experimental Decorators)

DefinitelyTyped, DefinitelyTyped, Tính sẵn có của kiểu (Type Availability)

interface dẫn xuất (derived interface), Mở rộng Giao diện (Extending Interfaces)

destructuring mảng, Thành viên mảng (Array Members)

destructuring đối tượng, Thuộc tính lồng nhau (Nested Properties)

discriminant, Discriminated Unions

discriminated unions

mô tả, Discriminated Unions

generics, Discriminated Unions generic (Generic Discriminated Unions)

tính phân phối của kiểu (distributivity), Tính phân phối của kiểu (Type Distributivity)

khai báo DOM (DOM declarations), Khai báo DOM (DOM Declarations)

định kiểu vịt (duck typing), Định kiểu theo hình dạng (Shapes and Structural Typing), Định kiểu cấu trúc (Structural Typing)

**E**

tùy chọn trình biên dịch emitDeclarationOnly, emitDeclarationOnly

xuất mã (emitting code)

tùy chọn emitDeclarationOnly, emitDeclarationOnly

xuất các tệp khai báo, Xuất các tệp khai báo (Emitting Declarations)

tùy chọn noEmit, noEmit

tùy chọn outDir, outDir

bản đồ nguồn (source maps), Source Maps

tùy chọn target, target

kiểu liệt kê (enums)

giá trị số tự động, Giá trị số tự động (Automatic Numeric Values)

const enums, Const Enums

mô tả, Kiểu liệt kê (Enums)

giá trị chuỗi, Enums có giá trị chuỗi (String-Valued Enums)

xử lý lỗi (error handling)

khẳng định kiểu lỗi bắt được, Khẳng định các kiểu lỗi bắt được (Asserting Caught Error Types)

lỗi của language service, Các lỗi của Language Service (Language Service Errors)

hiểu rõ các kiểu dữ liệu, Hiểu rõ các kiểu dữ liệu (Understanding types)

làm việc hiệu quả với các lỗi, Làm việc hiệu quả với các lỗi (Working Effectively with Errors)

tùy chọn trình biên dịch esModuleInterop, esModuleInterop, resolveJsonModule

evolving any, Chú thích kiểu (Type Annotations), Mảng evolving any (Evolving Any Arrays)

thuộc tính dư thừa (excess properties), Kiểm tra thuộc tính dư thừa (Excess Property Checks)

tùy chọn exclude, exclude

khẳng định kiểu tường minh, Khẳng định kiểu (Type Assertions)

kiểu gọi generic tường minh, Các kiểu gọi generic tường minh (Explicit Generic Call Types)

kiểu lớp generic tường minh, Các kiểu lớp generic tường minh (Explicit Generic Class Types)

kiểu tuple tường minh, Kiểu tuple tường minh (Explicit tuple types)

mở rộng cấu hình (extending configurations)

cấu hình cơ sở, Cấu hình cơ sở (Configuration Bases)

tùy chọn extends, extends

mở rộng lớp (extending classes), Mở rộng một Lớp (Extending a Class)

mở rộng giao diện (extending interfaces), Mở rộng Giao diện (Extending Interfaces)

khả năng gán của kế thừa (extension assignability), Khả năng gán của lớp kế thừa (Extension Assignability)

**F**

bao gồm tệp (file inclusions)

tùy chọn exclude, exclude

tùy chọn include, include

tìm kiếm triển khai (finding implementations), Tìm kiếm triển khai (Finding Implementations)

tìm kiếm định nghĩa (finding definitions), Tìm kiếm định nghĩa (Finding Definitions)

tìm kiếm tham chiếu (finding references), Tìm kiếm tham chiếu (Finding References)

hàm (functions)

hàm async, Hàm Async (Async Functions)

kiểu gọi generic tường minh, Các kiểu gọi generic tường minh (Explicit Generic Call Types)

thuộc tính hàm của lớp, Thuộc tính hàm (Function Properties)

nhiều tham số kiểu hàm, Nhiều tham số kiểu trong hàm (Multiple Function Type Parameters)

nạp chồng hàm (function overloads), Nạp chồng hàm (Function Overloads)

tham số (parameters), Tham số hàm (Function Parameters)

kiểu trả về (return types), Kiểu trả về (Return Types)

kiểu hàm (function types), Kiểu hàm (Function Types)

kiểu void, Kiểu trả về Void (Void Return Types)

kiểu never, Kiểu trả về Never (Never Return Types)

**G**

kiểu tổng quát (generics)

hàm generic, Các hàm generic (Generic Functions)

giao diện generic, Giao diện generic (Generic Interfaces)

lớp generic, Lớp generic (Generic Classes)

bí danh kiểu generic, Bí danh kiểu generic (Generic Type Aliases)

discriminated unions generic, Discriminated Unions generic (Generic Discriminated Unions)

bổ từ generic, Các bổ từ generic (Generic Modifiers)

giá trị mặc định cho generic, Giá trị mặc định cho Generic (Generic Defaults)

ràng buộc kiểu generic, Ràng buộc kiểu generic (Constrained Generic Types)

Promises và hàm async, Promises

quy tắc vàng của generics, Quy tắc vàng của Generics (The Golden Rule of Generics)

quy ước đặt tên cho generic, Quy ước đặt tên cho Generic (Generic Naming Conventions)

bổ sung toàn cục (global augmentations), Bổ sung toàn cục (Global Augmentations)

hợp nhất giao diện toàn cục (global interface merging), Hợp nhất giao diện toàn cục (Global Interface Merging)

các giá trị toàn cục (global values), Các giá trị toàn cục (Global Values)

bảng thuật ngữ (glossary), Bảng thuật ngữ (Glossary)

**I**

IDE (môi trường phát triển tích hợp), Sử dụng các tính năng IDE (Using IDE Features)

điều hướng mã nguồn, Điều hướng mã nguồn (Navigating Code)

viết mã và hoàn thành tự động, Viết mã (Writing Code)

hành động mã nguồn và tái cấu trúc, Hành động mã nguồn (Code Actions)

làm việc với lỗi, Làm việc hiệu quả với các lỗi (Working Effectively with Errors)

kiểu any ngầm định (implicit any), any, một lần nữa (any, Again), noImplicitAny

tùy chọn include, include

suy luận kiểu (type inference), Suy luận kiểu (Type Inference), Suy luận kiểu giao diện generic (Inferred Generic Interface Types)

các kiểu được suy luận (inferred types trong conditional types), Các kiểu được suy luận (Inferred Types)

kiểm tra khởi tạo (initialization checking), Kiểm tra khởi tạo (Initialization Checking), strictPropertyInitialization

chữ ký chỉ mục (index signatures), Index Signatures

giao diện (interfaces)

chữ ký cuộc gọi, Chữ ký cuộc gọi (Call Signatures)

giao diện và lớp, Lớp và Giao diện (Classes and Interfaces)

mở rộng giao diện, Mở rộng Giao diện (Extending Interfaces)

hợp nhất giao diện, Hợp nhất Giao diện (Interface Merging)

chữ ký chỉ mục, Index Signatures

giao diện generic, Giao diện generic (Generic Interfaces)

kiểu giao (intersection types), Kiểu giao (Intersection Types)

tùy chọn isolatedModules, isolatedModules

**J**

JavaScript

biên dịch sang JavaScript, Bắt đầu với TypeScript (Getting Started with TypeScript), Xuất mã (Emit)

tùy chọn allowJs, allowJs

tùy chọn checkJs, checkJs

hỗ trợ JSDoc, Hỗ trợ JSDoc (JSDoc Support)

chú thích @ts-check, @ts-check

cú pháp JSX, Cú pháp JSX (JSX Syntax)

tùy chọn jsx, jsx

**K**

toán tử keyof, keyof, keyof và các tham số kiểu có ràng buộc (keyof and Constrained Type Parameters)

toán tử typeof, typeof, keyof typeof

**L**

các khai báo thư viện (library declarations), Khai báo thư viện (Library Declarations)

mục tiêu thư viện, Mục tiêu thư viện (Library targets)

tùy chọn lib, lib

kiểu literal (literal types), Kiểu Literal (Literal Types)

khả năng gán của literal, Khả năng gán Literal (Literal Assignability)

literals sang primitives, Từ Literals đến Primitives (Literals to Primitives)

**M**

kiểu ánh xạ (mapped types)

mô tả, Kiểu ánh xạ (Mapped Types)

từ các kiểu khác, Mapped Types từ các kiểu khác (Mapped Types from Types)

thay đổi các bổ từ, Thay đổi các bổ từ (Changing Modifiers)

mapped types tổng quát, Mapped Types tổng quát (Generic Mapped Types)

mapped conditional types, Mapped Conditional Types

never và mapped types, never và Mapped Types

ánh xạ lại các khóa, Ánh xạ lại các khóa của Mapped Type (Remapping Mapped Type Keys)

phạm vi hiển thị của thành viên (member visibility), Phạm vi hiển thị của thành viên (Member Visibility)

các module (modules)

mô tả, Các Module (Modules)

tùy chọn module, module

phân giải module (module resolution), moduleResolution

khả năng tương tác với CommonJS, Khả năng tương tác với CommonJS (Interoperability with CommonJS)

khai báo module, Khai báo Module (Module Declarations)

khai báo module đại diện (wildcard), Khai báo Module đại diện (Wildcard Module Declarations)

chỉ import và export kiểu, Chỉ Import và Export Kiểu (Type-Only Imports and Exports)

mảng đa chiều (multidimensional arrays), Mảng đa chiều (Multidimensional Arrays)

**N**

không gian tên (namespaces)

mô tả, Không gian tên (Namespaces)

export trong namespace, Export trong Namespace (Namespace Exports)

namespaces lồng nhau, Namespaces lồng nhau (Nested Namespaces)

namespaces trong định nghĩa kiểu, Namespaces trong định nghĩa kiểu (Namespaces in Type Definitions)

ưu tiên modules hơn namespaces, Ưu tiên Modules hơn Namespaces (Prefer Modules Over Namespaces)

kiểu never

mô tả, Kiểu đáy never (The never Bottom Type), never

kiểu trả về never, Kiểu trả về Never (Never Return Types)

never và kiểu giao/hợp, never và các phép giao và hợp (never and Intersections and Unions)

never và kiểu điều kiện, never và Kiểu điều kiện (never and Conditional Types)

never và mapped types, never và Mapped Types

tùy chọn noEmit, noEmit

tùy chọn noImplicitAny, noImplicitAny

khẳng định không null (non-null assertions), Khẳng định không null (Non-Null Assertions)

kiểu null và undefined, Kiểm tra null nghiêm ngặt (Strict Null Checking)

kiểm tra null nghiêm ngặt, Kiểm tra null nghiêm ngặt (Strict Null Checking), strictNullChecks

**O**

đối tượng (objects)

kiểu đối tượng, Kiểu đối tượng (Object Types)

thuộc tính lồng nhau, Thuộc tính lồng nhau (Nested Properties)

kiểm tra thuộc tính dư thừa, Kiểm tra thuộc tính dư thừa (Excess Property Checks)

discriminated unions, Discriminated Unions

kiểu giao, Kiểu giao (Intersection Types)

đối tượng chỉ đọc, Các đối tượng chỉ đọc (Read-Only Objects)

thuộc tính tùy chọn (optional properties), Thuộc tính tùy chọn (Optional Properties)

tham số tùy chọn (optional parameters), Tham số tùy chọn (Optional Parameters)

nạp chồng hàm (overloaded functions), Nạp chồng hàm (Function Overloads)

ghi đè (overriding)

ghi đè hàm khởi tạo, Ghi đè hàm khởi tạo (Overridden Constructors)

ghi đè phương thức, Ghi đè phương thức (Overridden Methods)

ghi đè thuộc tính, Ghi đè thuộc tính (Overridden Properties)

**P**

kiểu gói (package types), Kiểu của các gói (Package Types)

kiểu của gói phụ thuộc, Kiểu của gói phụ thuộc (Dependency Package Types)

công khai kiểu của gói, Công khai kiểu của gói (Exposing Package Types)

thuộc tính tham số (parameter properties), Thuộc tính tham số của lớp (Class Parameter Properties)

tham số (parameters), Tham số hàm (Function Parameters)

tham số bắt buộc, Tham số bắt buộc (Required Parameters)

tham số tùy chọn, Tham số tùy chọn (Optional Parameters)

tham số mặc định, Tham số mặc định (Default Parameters)

tham số rest, Tham số Rest (Rest Parameters)

kiểu nguyên thủy (primitives), Có gì trong một Kiểu? (What's in a Type?)

tùy chọn preserveConstEnums, Const Enums

tham chiếu dự án (project references)

mô tả, Tham chiếu dự án (Project References)

tùy chọn composite, composite

tùy chọn references, references

chế độ build, Chế độ Build (Build Mode)

Promises, Promises, Tạo Promises (Creating Promises)

**Q**

sửa lỗi nhanh (quick fixes), Các sửa lỗi nhanh khác (Other quick fixes)

**R**

thuộc tính chỉ đọc (readonly properties), Thuộc tính chỉ đọc (Read-Only Properties)

đối tượng chỉ đọc, Các đối tượng chỉ đọc (Read-Only Objects)

tái cấu trúc (refactoring), Tái cấu trúc (Refactoring)

tùy chọn references trong TSConfig, references

đổi tên biểu tượng (renaming symbol), Đổi tên (Renaming)

tùy chọn resolveJsonModule, resolveJsonModule

kiểu trả về (return types), Kiểu trả về (Return Types)

kiểu trả về void, Kiểu trả về Void (Void Return Types)

kiểu trả về never, Kiểu trả về Never (Never Return Types)

**S**

bản đồ nguồn (source maps), Source Maps

tùy chọn sourceMap, sourceMap

tùy chọn declarationMap, declarationMap

toán tử spread, Spreads, Tham số Rest (Rest Parameters)

chế độ nghiêm ngặt (strict mode)

mô tả, Chế độ nghiêm ngặt (Strict Mode)

noImplicitAny, noImplicitAny

strictBindCallApply, strictBindCallApply

strictFunctionTypes, strictFunctionTypes

strictNullChecks, strictNullChecks

strictPropertyInitialization, strictPropertyInitialization

useUnknownInCatchVariables, useUnknownInCatchVariables

định kiểu cấu trúc (structural typing), Định kiểu theo hình dạng (Shapes and Structural Typing), Định kiểu cấu trúc (Structural Typing)

lớp con (subclasses), Mở rộng một Lớp (Extending a Class)

phần mở rộng cú pháp (syntax extensions), Phần mở rộng cú pháp (Syntax Extensions)

thuộc tính tham số của lớp, Thuộc tính tham số của lớp (Class Parameter Properties)

decorators thử nghiệm, Decorators thử nghiệm (Experimental Decorators)

enums, Kiểu liệt kê (Enums)

namespaces, Không gian tên (Namespaces)

chỉ import và export kiểu, Chỉ Import và Export Kiểu (Type-Only Imports and Exports)

**T**

tùy chọn target, target

kiểu template literal (template literal types)

mô tả, Kiểu Template Literal (Template Literal Types)

các kiểu thao tác chuỗi nội tại, Các kiểu thao tác chuỗi nội tại (Intrinsic String Manipulation Types)

các khóa template literal, Các khóa Template Literal (Template Literal Keys)

ánh xạ lại các khóa của mapped type, Ánh xạ lại các khóa của Mapped Type (Remapping Mapped Type Keys)

kiểu đỉnh (top types), Kiểu đỉnh (Top Types)

any, any, một lần nữa (any, Again)

unknown, unknown

chuyển mã (transpilation), Bắt đầu với TypeScript (Getting Started with TypeScript), Xuất mã (Emit)

tệp TSConfig, Các tệp TSConfig (TSConfig Files)

tsc --init, tsc --init

tùy chọn tsc, Tùy chọn tsc (tsc Options)

tuples, Tuples

kiểu dữ liệu (types)

chú thích kiểu (type annotations), Chú thích kiểu (Type Annotations)

bí danh kiểu (type aliases), Bí danh kiểu (Type Aliases)

khẳng định kiểu (type assertions), Khẳng định kiểu (Type Assertions)

bộ bảo vệ kiểu (type guards), Bộ bảo vệ kiểu (Type Guards)

thu hẹp kiểu (type narrowing), Thu hẹp kiểu (Type Narrowing)

thao tác kiểu (type operations), Các thao tác kiểu (Type Operations)

vị từ kiểu (type predicates), Vị từ kiểu (Type Predicates)

toán tử kiểu (type operators), Toán tử kiểu (Type Operators)

hệ thống kiểu (type system), Hệ thống kiểu (The Type System)

kiểm tra kiểu (type checking), Kiểm tra kiểu (Type Checking)

chỉ import và export kiểu (type-only imports and exports), Chỉ Import và Export Kiểu (Type-Only Imports and Exports)

toán tử typeof, typeof, keyof typeof

**U**

kiểu undefined, Kiểm tra null nghiêm ngặt (Strict Null Checking)

kiểu hợp (union types), Kiểu hợp (Union Types)

mảng kiểu hợp, Mảng kiểu hợp (Union-Type Arrays)

kiểu unknown, unknown, useUnknownInCatchVariables

tùy chọn useUnknownInCatchVariables, useUnknownInCatchVariables

**V**

phạm vi hiển thị của thành viên (visibility), Phạm vi hiển thị của thành viên (Member Visibility)

kiểu void, Kiểu trả về Void (Void Return Types)

**W**

chế độ watch (watch mode), Chế độ Watch (Watch Mode)

khai báo module đại diện (wildcard module declarations), Khai báo Module đại diện (Wildcard Module Declarations)
